def calculate_city_avg_pollutions(cities_list):
    # for city in cities_list:
    #
    #
    # actual_filename = "weather_" + city + ".csv"
    # forecast_filename = "forecast_" + city + ".csv"
    #
    # city_actual_df = pd.read_csv(actual_filename)
    # city_forecast_df = pd.read_csv(forecast_filename)
    pass

def main():
    variables = ["pm10", "pm2_5", "carbon_monoxide", "nitrogen_dioxide", "sulphur_dioxide"]
    cities_us = ["Phoenix", "Philadelphia", "San_Antonio", "San_Diego", "Dallas"]
    cities_eu = ["Budapest", "Barcelona", "Munich", "Prague", "Milan"]

if __name__ == "__main__":
    main()